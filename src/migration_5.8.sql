-- ============================================================
-- Миграция 5.8 — колонка disabled в api_neighbours.
-- Позволяет временно отключить сервер-источник без удаления
-- записи: все исходящие запросы (тест, поиск, push, офферы)
-- к отключённому серверу не отправляются.
-- ============================================================

ALTER TABLE api_neighbours ADD COLUMN IF NOT EXISTS disabled BOOLEAN NOT NULL DEFAULT FALSE;
